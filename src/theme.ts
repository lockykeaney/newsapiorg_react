import { Theme } from "@emotion/react";

const theme = {
  fonts: {
    heading: {
      fontFamily: '"Open Sans", sans-serif',
      fontWeight: 500,
      fontSize: "1rem",
      lineHeight: "1rem",
      margin: 0,
      padding: 0,
    },
  },
};

export default theme;
